import { ModulePlaceholder } from "@/components/layout/module-placeholder";

export default function SettingsPage() {
  return <ModulePlaceholder title="Settings center" description="Theme, notification, profile, and security settings will be implemented once auth and data layers are added." />;
}
